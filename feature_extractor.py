# features.py

def extract_features(url):
    # Example feature extractor (replace with your actual logic)
    return {
        "url_length": len(url),
        "has_https": int("https" in url),
        # Add more features here as per your training
    }

